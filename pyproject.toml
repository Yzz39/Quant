[project]
name = "quant"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "ipykernel",
    "jupyter",
    "matplotlib",
    "numpy",
    "openpyxl",
    "pandas",
]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
]
